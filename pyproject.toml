[build-system]
requires = ["setuptools"]
build-backend = "setuptools.build_meta"

[tool.ruff]
line-length = 120

[tool.ruff.lint.pycodestyle]
max-line-length = 120

[tool.ruff.format]
skip-magic-trailing-comma = true
